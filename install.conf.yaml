- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/tmux: tmux
    ~/.config/alacritty: alacritty

- shell:
  - [git submodule update --init --recursive, Installing submodules]
